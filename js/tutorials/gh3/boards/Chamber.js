var React = require('react'),
    $ = require('jquery'),
    PubSub = require('pubsub-js');

var Chamber = React.createClass({
    render: function() {
        return (
            <div>CHAMBER</div>
        );
    }
});

module.exports = Chamber;